name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        path: android

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Prepare & Force Dependencies
      run: |
        # 1. التجهيز المعتاد
        npm init -y
        npm install @capacitor/android @capacitor/core @capacitor/cli
        
        echo '{"appId":"com.ajwan.app","appName":"Ajwan","webDir":"www","bundledWebRuntime":false}' > capacitor.config.json
        mkdir www && echo '<h1>Ajwan</h1>' > www/index.html
        echo '{ "project_info": { "project_number": "0", "project_id": "m", "storage_bucket": "m" }, "client": [{ "client_info": { "mobilesdk_app_id": "1:0:android:0", "android_client_info": { "package_name": "com.ajwan.app" } }, "api_key": [{ "current_key": "m" }] }] }' > android/app/google-services.json

        npx cap sync android

        # =======================================================
        # 🚨 القبضة الحديدية: إجبار التوافق (Force Resolution) 🚨
        # سنحقن كوداً في build.gradle يجبره على استخدام نسخ محددة
        # =======================================================
        
        cat <<EOG >> android/build.gradle

        allprojects {
            configurations.all {
                resolutionStrategy {
                    // إجبار استخدام إصدارات مستقرة ومعروفة
                    force 'androidx.core:core:1.12.0'
                    force 'androidx.core:core-ktx:1.12.0'
                    force 'androidx.appcompat:appcompat:1.6.1'
                    force 'androidx.activity:activity:1.8.0'
                    force 'androidx.fragment:fragment:1.6.2'
                    force 'androidx.lifecycle:lifecycle-viewmodel:2.6.2'
                    force 'androidx.lifecycle:lifecycle-viewmodel-savedstate:2.6.2'
                    force 'androidx.coordinatorlayout:coordinatorlayout:1.2.0'
                    force 'androidx.work:work-runtime:2.9.0'
                    force 'com.google.android.material:material:1.10.0'
                }
            }
        }
        EOG

        # 2. ترقية AGP و Gradle (ضروري جداً)
        sed -i 's/classpath "com.android.tools.build:gradle:.*"/classpath "com.android.tools.build:gradle:8.3.2"/g' android/build.gradle
        sed -i 's/8.2/8.7/g' android/gradle/wrapper/gradle-wrapper.properties

        # 3. التأكد من SDK 34
        cat <<EOV > android/variables.gradle
        ext {
            minSdkVersion = 23
            compileSdkVersion = 34
            targetSdkVersion = 34
            androidxActivityVersion = '1.8.0'
            androidxAppCompatVersion = '1.6.1'
            androidxCoordinatorLayoutVersion = '1.2.0'
            androidxCoreVersion = '1.12.0'
            androidxFragmentVersion = '1.6.2'
            coreSplashScreenVersion = '1.0.1'
            androidxJunitVersion = '1.1.5'
            androidxEspressoCoreVersion = '3.5.1'
            cordovaAndroidVersion = '10.1.1'
        }
        EOV
        
        # 4. إصلاح مكتبات الكابستور
        find node_modules -name build.gradle -exec sed -i 's/VERSION_21/VERSION_17/g' {} +

    - name: Build with Gradle
      run: |
        cd android
        chmod +x gradlew
        # --stacktrace + --info لنرى ماذا يحدث بالتفصيل
        ./gradlew assembleDebug -x lint --no-daemon --no-parallel -g ./local_cache --stacktrace

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Ajwan-App-Debug
        path: android/app/build/outputs/apk/debug/app-debug.apk
